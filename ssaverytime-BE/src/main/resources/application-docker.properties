# Docker 환경 전용 설정
spring.application.name=ssaverytime

# DB 설정 (컨테이너 이름 사용)
spring.datasource.url=jdbc:mysql://ssaverytime-mysql:3306/ssaverytime?serverTimezone=UTC&useUniCode=yes&characterEncoding=UTF-8
spring.datasource.username=ssafy
spring.datasource.password=ssafy
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Actuator & Prometheus (모니터링용)
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=always
management.metrics.tags.application=${spring.application.name}

# JWT
jwt.secret=THIS_IS_A_VERY_SECRET_KEY_32_BYTES_MINIMUM_LENGTH!!
jwt.access-expiration-ms=3600000
jwt.refresh-expiration-ms=1209600000

# Logging
logging.level.com.ssafy=trace
logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.springframework.jdbc.datasource=DEBUG
